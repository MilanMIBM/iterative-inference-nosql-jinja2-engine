{
    "set_id": "7658cf1b-fa0c-470c-8822-7ad119e77498",
    "name": "llm_kv_detection_system_templates_v1",
    "description": "Iterative keyvalue extraction system template that is meant to reiterate over its previous work with the goal of being able to extract keyvalue pairs even in low resolution images.",
    "system_templates": {
        "system_prompt": "Utilize the provided input image to extract key-value pairs in the provided image based on proximity. Output results must only include the resulting YAML data structure encased between ----- | \n<result>\n | ----- delimiters around it,\n\n{% if output_structure_example %}Reference Output Structure Example:\n\n {{ output_structure_example }}\n {% endif %}",
        "output_structure_example": "----- | \nkey: value\nkey2: value2\nkey3: value3\n ... | -----",
        "instruction_prompt": "Extract key-value pairs from the image input as YAML values.",
        "next_question_user_message": {
            "role": "user",
            "content": [
                {
                    "type": "text",
                    "text": "{% if instruction_prompt %} {{ instruction_prompt }} {% endif %}"
                },
                {
                    "type": "image_url",
                    "image_url": {
                        "url": "{% if image_data_url %}{{ image_data_url }}{% endif %}"
                    }
                }
            ]
        }
    }
}